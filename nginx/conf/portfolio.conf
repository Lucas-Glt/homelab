server {
    listen 80;
    server_name me.espacenum.fr;

    # Indispensable pour Certbot
    location /.well-known/acme-challenge/ {
        root /usr/share/nginx/html;
    }

    # Redirection vers HTTPS (À ne décommenter QU'APRÈS avoir généré le SSL)
     location / {
         return 301 https://$host$request_uri;
     }  
        
}

server {
    listen 443 ssl;

    server_name me.espacenum.fr;

    
    ssl_certificate /etc/letsencrypt/live/me.espacenum.fr/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/me.espacenum.fr/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
