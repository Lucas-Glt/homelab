services:
    uptime-kuma:
      image: louislam/uptime-kuma:1
      container_name: kuma
      restart: unless-stopped
      volumes:
        - /srv/uptime-kuma:/app/data
      networks:
        - web

networks:
  web:
    external: true

